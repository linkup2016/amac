/*
package com.yonasamare.amacmembershipmanager.mappers;

import com.yonasamare.amacmembershipmanager.records.MembershipApplication;

public class MemberMapper {
    public Member mapMembershipApplicationToMember(MembershipApplication application) {
        Member member = new Member();
        member.setFirstName(application.getFirstName());
        member.setLastName(application.getLastName());
        member.setStreetAddress(application.getStreetAddress());
        member.setApartment(application.getApartment());
        member.setCity(application.getCity());
        member.setState(application.getState());
        member.setZipCode(application.getZipCode());
        member.setPhoneNumber(application.getPhoneNumber());
        member.setEmail(application.getEmail());
        return member;
    }
}

*/
